describe('JavaScript addition operator', function () {
    it('adds two numbers together', function () {
        // import GtEventManager from 'components/GtEventManager/GtEventManager';
        // var events = new GtEventManager();
        // console.log(events);
        expect(1 + 2).toEqual(3);
    });
});